server:
  port: 8081
spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
      - id: orderService
        uri: http://localhost:8085/
        predicates:
        - Path=/orders/v1/**